# 실시간 진행형 퀴즈 웹앱 — 새 주제로 다시 만들기

아래는 "퀴즈로 알아보는 정읍" 앱을 만들며 확정된 사양입니다. 컨텐츠(문제·주제·팀 구성)만 바꿔서 같은 구조로 새로 만들어주세요.

## 무엇을 만드는가

시민 원탁회의/워크숍 같은 오프라인 행사에서, 진행자가 무대에서 문제를 띄우면 각 테이블 담당자가 휴대폰으로 답하고, 진행자가 정답을 공개하면 팀별 정답/오답과 누적 순위가 나오는 실시간 퀴즈. 화면은 두 개 — 진행자용(빔프로젝터), 팀 참여자용(QR로 접속하는 휴대폰).

## 기술 스택

- Node.js 내장 `http` 모듈만 사용 (Express 등 프레임워크 없음), 의존성은 `qrcode`와 `cloudflared`(npm 패키지) 둘뿐
- 실시간 동기화는 **SSE(Server-Sent Events) + 폴링 폴백**. Cloudflare quick tunnel이 SSE 본문을 버퍼링해서 실시간 스트림이 안 오는 경우가 있어, 프론트에서 2.5초 안에 첫 메시지가 안 오면 자동으로 600ms 폴링(`/api/state`)으로 전환하게 만들어야 함
- 인터넷 공개는 `cloudflared` npm 패키지의 quick tunnel(`Tunnel.quick(url)`, v0.7 기준 이벤트 API 사용 — `t.on('url', ...)`, `t.on('exit', ...)`). **quick tunnel 주소가 프로세스는 살아있는데 조용히 죽는 경우가 있어** exit 이벤트에서 자동 재연결 로직을 넣고, 행사 전 반드시 실제 접속 테스트 필요
- 상태는 서버 메모리 + `state.json` 파일에 디바운스 저장 (서버 재시작해도 복구됨)
- 프론트는 순수 HTML/CSS/JS, 빌드 과정 없음

## 서버가 관리하는 상태 (핵심 개념)

```
phase: lobby | quiz | outro | final
index: 지금 몇 번 문항인지 (연습문제 포함 전체 목록의 인덱스)
open: 지금 답변을 받고 있는지
revealed: 정답이 공개됐는지
answers: { 문항ID: { 팀번호: {choice, ms(문항 열린 뒤 답할 때까지 걸린 시간)} } }
joined: { 팀번호: 처음 접속한 시각 } — 한 번이라도 들어온 팀. 접속 판정과 순위 분모는 전부 이걸 기준으로 함
asked: { 문항ID: true } — 한 번이라도 출제된 문항 (진행 버튼이 되돌아가지 않게)
```

## 진행 흐름 (여러 번 반복하며 다듬어진 최종 형태)

1. **대기화면(lobby)** — QR + 테이블 번호 원형 목록. 참가자가 QR 찍고 자기 팀 번호 누르면 참여.
2. **문항 선택 = 즉시 출제.** 별도의 "출제" 버튼도 타이머도 없음. 문항을 고르는 순간 `open=true`.
3. 팀은 마감 전까지 답을 몇 번이든 바꿀 수 있음.
4. **"정답 공개"를 누르는 그 순간 답변이 마감**됨 (별도의 "마감" 단계 없음). 마감 후 답변 시도는 409로 거부.
5. 진행자 화면 큰 버튼 하나가 상태에 따라 자동으로 문구가 바뀜: 문제 시작 → 정답 공개 → 다음 문제 → (마지막 문항 후) 최종 순위 → 마무리 문구 → 대기화면으로. 이 버튼 하나만 눌러도 끝까지 진행 가능. `Space` 키도 동일 동작.
6. **최종 순위는 연습문제를 제외한 본문제만 집계.** 동점이면 정답을 더 빨리 누른 팀이 앞섬 — **공동 순위 없이 반드시 1등부터 끝까지 갈라야 함** (score desc, tie-break ms asc, rank = 그냥 배열 인덱스+1).
7. 마지막에 마무리 멘트 페이지.

## 접속·집계 관련 중요 규칙

- **분모는 항상 "실제로 들어온 팀" 수.** 전체 팀 10개 중 4개만 왔으면 "제출 2 / 4팀"으로 표시하고 나머지 6팀은 기다리지 않음. 아무도 안 답해도 그냥 다음으로 넘어갈 수 있어야 함(기권 허용).
- 참여한 팀이 전원 답하면 진행 버튼이 강조 애니메이션으로 깜빡임.
- **팀 접속 표시는 "실시간 연결"이 아니라 "한 번이라도 들어온 적 있음" 기준.** 휴대폰 화면이 꺼져도 매번 다시 어두워지면 안 됨.
- **전체 초기화는 답변·점수뿐 아니라 접속 팀 목록까지 완전히 지워야 함.** (리허설 후 실전 전 클리어용)
- 화면이 꺼졌다 다시 QR로 들어와도 그 팀의 답변·점수는 서버에 남아 그대로 이어짐.
- 참여 QR은 **진행자 화면을 지금 연 주소를 그대로 인코딩**. 하드코딩된 단축주소를 쓰지 말 것 — 서버를 재시작하면 터널 주소가 매번 바뀌므로, 진행자 화면을 새 주소로 열면 QR도 자동으로 맞춰지는 구조가 훨씬 안전함(저장된 주소가 현재 origin과 다르면 자동 폐기하고 현재 주소로 복귀).

## 진행자 화면 UI

- 상단 타이틀 바 없음 — 진행률/연결 상태는 하단 조작 바에 배지로.
- 문항은 **가운데 정렬**, 보기는 **가로 3등분** 카드(연습문제 아님을 배지로 표시하되 "점수 미반영" 같은 문구는 화면에 노출하지 않음).
- 팀 참여 현황은 원형 칩, **5개씩 2줄**로 가로 폭을 꽉 채움. 접속하면 시그니처 옐로우로 켜짐, 답 제출하면 체크 표시.
- 정답 공개 화면: 왼쪽에 보기별 결과(가로로 눕힌 카드) + 팀별 O/X 칩, 오른쪽에 **정답은 진한 옐로우 카드로 크게**, 그 아래 **핵심 숫자 + 해설은 연한 그레이 카드로 분리**(문장 단위로 줄바꿈, 카드 안에 안 들어가면 자동으로 글자 크기를 줄여서라도 잘리지 않게).
- 최종 순위: 2열 카드 목록, 점수 숫자를 아주 크게(팀명·순위보다 확연히 큼), 1위는 옐로우 카드로 강조.
- **전체화면 버튼은 네이티브 Fullscreen API와 별개로 "무대 모드" CSS 클래스를 같이 둠** — 브라우저가 전체화면 요청을 거부해도(임베디드 미리보기 등) 상단 UI는 확실히 숨겨지도록.
- 자주 안 쓰는 버튼(대기화면 전환·최종 순위 바로가기·초기화)은 "더보기"로 접어 조작 바를 한 줄로 유지.
- 연결이 끊기면 화면 어디서나 보이는 위치에 "연결 끊김" 뱃지.
- 문항 선택 드롭다운은 새로고침 시 브라우저가 이전 값을 복원하며 `change` 이벤트를 잘못 쏘는 경우가 있어, 현재 인덱스와 같으면 무시하도록 방어.

## 팀(참여자) 화면 UI

- 모바일 전용. 접속하면 팀 번호 선택 화면 → 문항 화면 → 정답 공개 화면을 서버 상태에 따라 그대로 따라감(참여자는 스스로 진행 안 함, 답만 누름).
- 정답 공개 시 우리 팀 O/X, 현재 누적 점수, 순위를 함께 보여줌.
- 최종 순위 페이지에서는 자기 팀 순위와 메달 이모지.

## 색상 — 이번엔 "○○교육연구소" 브랜드 톤을 실제 웹사이트에서 뽑아 씀

**절대 짐작하지 말고, 브랜드/기관 홈페이지가 있으면 브라우저로 열어서 `getComputedStyle`로 실제 배경색/텍스트색 빈도를 세어 확인할 것.** (WebFetch 텍스트 요약은 색을 틀리게 짚는 경우가 있었음.) 이번 프로젝트에서 확정된 팔레트:

| 역할 | 값 |
|---|---|
| 진행자 화면 배경(다크) | `#2C2C2A` / 패널 `#3A3A38` |
| 시그니처 강조색(옐로우) | `#FDCE0F` — 버튼·접속 표시·정답 카드 등 **작은 면적에만** |
| 넓은 면(배지·팀 순위 1위 등) | 크림 `#FDF2DC` — 순수 옐로우를 넓게 쓰면 너무 쨍해서 크림으로 대체 |
| 설명/해설 카드 | 연한 그레이 `#ECECEC` (배경) + `#3A3A38`(글씨) — 정답 색과 분리해야 가독성이 좋아짐 |
| 팀 화면 배경 | 진행자와 통일 (다크 + 옐로우), 넓은 면은 마찬가지로 크림/연그레이 |

**새 주제로 만들 때는 이 표를 참고만 하고, 새 브랜드 컬러가 있으면 그 기관 사이트에서 다시 실측할 것.**

## 데이터 파일 구조 (JSON 하나로 컨텐츠 관리)

```json
{
  "meta": { "title": "", "event": "", "date": "", "venue": "", "host": "", "teamCount": 10, "scoredCount": 10 },
  "practice": { "id": "P0", "scored": false, "question": "", "choices": ["","",""], "answerIndex": 0, "answerLabel": "", "highlight": "", "explanation": "", "source": "" },
  "questions": [ /* practice와 같은 형태 + table, agendaNo, agenda, id: "Q1".. */ ],
  "outro": { "title": "", "lines": ["", "", ""] },
  "agendas": [ { "no": 1, "short": "", "name": "" } ],
  "teams": [ { "no": 1, "label": "화면에 보일 번호(예: 1-1)", "agendaNo": 1, "agenda": "" } ]
}
```

`teams[].no`는 내부 식별자라 절대 바꾸면 안 되고(답변·점수가 매핑됨), `label`만 자유롭게 바꿔 화면 표시용 번호를 관리. 팀 번호는 **테이블 배치도가 있으면 그 배치도의 번호 체계(예: 의제 1의 테이블이 둘이면 1-1/1-2)를 그대로 따를 것** — 참가자가 실제 앉은 테이블 번호와 화면 번호가 일치해야 헷갈리지 않음.

## 배포/운영 방식

- `npm run live` (= `node server.js --tunnel`)로 로컬 서버 + cloudflared quick tunnel을 동시에 켬. 실행할 때마다 인터넷 주소가 새로 발급되므로 **행사 시작 전 한 번 켜고 그 창을 절대 닫지 않기**.
- 진행자 화면은 반드시 그 터널 주소로 열어야 QR도 같은 주소를 가리킴.
- 같은 와이파이만 쓸 거면 `npm start`로 로컬 IP 접속도 가능(단, 공용 와이파이의 기기 격리로 막힐 수 있어 인터넷 경유가 더 안전).
- 더블클릭 실행용 `.bat` 파일 하나 만들어 두면 비개발자도 실행 가능.
- git 저장소로 초기화해두고 커밋 남기면 이후 수정 히스토리 추적 가능.

## 테스트 방식 (Node 스크립트로 서버 API를 직접 두드려 회귀 검증)

브라우저 스크린샷 없이도, `/api/host/*`와 `/api/answer`를 curl/fetch로 순서대로 호출해 상태 전이·채점·경계 조건(미접속 팀 제외, 마감 후 답변 거부, 동점 타이브레이크 등)을 자동으로 검증하는 스크립트를 만들어 두면 화면을 바꿀 때마다 로직이 안 깨졌는지 빠르게 확인 가능. 실제 화면 레이아웃(글자 넘침 등)은 브라우저로 각 상태를 강제로 만들어(`/api/host/select`, `/api/host/reveal` 직접 호출) `scrollHeight - clientHeight`로 넘침 여부를 재는 방식이 효과적이었음.

---

**이 문서를 새 대화에 붙여넣고, 새 주제/컨텐츠(JSON 데이터)만 알려주면 같은 구조로 새로 만들 수 있습니다.**
